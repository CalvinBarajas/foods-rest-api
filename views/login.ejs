<%- include('partials/header') %>

<div class="container mt-5">
  <h1 class="display-3">Foods REST API</h1>
  <i class="fas fa-carrot fa-4x carrot"></i>
  <i class="fas fa-apple-alt fa-4x apple-alt"></i>
  <i class="fas fa-lemon fa-4x lemon"></i>
  <h1 class="header-h1">Login</h1>

  <!-- EJS to load message in case acct already exists, does not exist, etc. -->
  <% if (locals.msg) { %>
  <h2 class="carrot"><%= msg %></h2>
  <% } %>

  <div class="row">
    <div class="col-sm-8">
      <div class="card">
        <div class="card-body">
          <!-- Makes POST request to /login route -->
          <form action="/api/user/login" method="POST">
            <div class="form-group">
              <label for="username">Email</label>
              <input type="username" class="form-control" name="username" placeholder="Email Address" required>
            </div>
            <div class="form-group">
              <label for="password">Password</label>
              <input type="password" class="form-control" name="password" placeholder="Password" required>
            </div>
            <button type="submit" class="btn btn-info">Login</button>
            <a class="btn btn-dark btn-md" href="/" role="button"> Home</a>
          </form>
        </div>
      </div>
    </div>
  </div>
  <hr />
  <h1><strong>Features:</strong></h1>
  <ul>
    <li>Visitors will be redirected to API key after successfully logging in.</li>
    <li>Passport and Express-Session work together to write ID to cookie.</li>
    <li>Converts upper-case emails to lower-case automatically (middleware).</li>
    <li>One layer of validation (HTML form).</li>
    <li>Detects if acct does not exist in db.</li>
  </ul>

</div>

<%- include('partials/footer') %>