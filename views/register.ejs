<%- include('partials/header') %>

<div class="container mt-5">
  <h1 class="display-3">Foods REST API</h1>
  <i class="fas fa-carrot fa-4x carrot"></i>
  <i class="fas fa-apple-alt fa-4x apple-alt"></i>
  <i class="fas fa-lemon fa-4x lemon"></i>
  <h1 class="header-h1">Register</h1>

  <!-- EJS to load message in case acct already exists, does not exist, etc. -->
  <% if (locals.msg) { %>
  <h2 class="carrot"><%= msg %></h2>
  <% } %>

  <div class="row">
    <div class="col-sm-8">
      <div class="card">
        <div class="card-body">
          <!-- Makes POST request to /register route -->
          <form action="/api/user/register" method="POST">
            <div class="form-group">
              <label for="username">Email</label>
              <input type="email" class="form-control" name="username" placeholder="Minimum 6 Characters" required />
            </div>
            <div class="form-group">
              <label for="password">Password</label>
              <input type="password" class="form-control" placeholder="Minimum 6 Characters" name="password" required />
            </div>
            <button type="submit" class="btn btn-info">Register</button>
            <a class="btn btn-dark btn-md" href="/" role="button"> Home</a>
          </form>
        </div>
      </div>
    </div>
  </div>
  <hr />
  <h1><strong>Features:</strong></h1>
  <ul>
    <li>Two layers of validation (html form and Joi.dev).</li>
    <li>Detects duplicate accounts (passport-local-mongoose).</li>
    <li>Password encryption (passport-local-mongoose).</li>
    <li>Converts upper-case emails to lower-case automatically (middleware).</li>
    <li>Info/warning/error messages displayed (e.g., dupe emails, short emails, short passwords, etc.).</li>
  </ul>
</div>

<%- include('partials/footer') %>
